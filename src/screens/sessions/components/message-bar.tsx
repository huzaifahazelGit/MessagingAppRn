{
  /* <React.Fragment>
<Image
  source={{
    uri: myInfo?.profilePicture,
  }}
  style={styles.messageAvatar}
/>
<View>
  <TextInput
    onFocus={() => {
      stopAnimation();
    }}
    onBlur={() => {
      setState({});
    }}
    clearTextOnFocus={true}
    onSubmitEditing={_onSendMessage}
    returnKeyType="send"
    textAlignVertical="center"
    placeholder="Send a message"
    placeholderTextColor="#fff"
    onChangeText={(e) => (ref.current.message = e)}
    style={styles.messageInput}
  />
  <TouchableOpacity
    onPress={() => {
      setShowOptions(true);
      stopAnimation();
    }}
    style={styles.btnOptions}
  >
    <Icon name="dots-vertical" color="#fff" size={24} />
  </TouchableOpacity>
</View>
<TouchableOpacity style={styles.btnSend}>
  <Icon name="send" size={36} color="#fff" />
</TouchableOpacity>
</React.Fragment> */
}

{
  /* <TouchableOpacity
                    onPress={() => {
                      stopAnimation();
                      //   navigate("AddToHighlights", {
                      //     superId: sessions[childIndex].source,
                      //     uid: sessions[childIndex].uid,
                      //   });
                    }}
                    style={{
                      justifyContent: "center",
                      alignItems: "center",
                      marginRight: 15,
                    }}
                  >
                    <View
                      style={{
                        height: 30,
                        width: 30,
                        borderRadius: 30,
                        borderColor: "#fff",
                        borderWidth: 2,
                        borderStyle: "dashed",
                        marginBottom: 5,
                        justifyContent: "center",
                        alignItems: "center",
                      }}
                    >
                      <Icon name="heart" size={14} color="#fff" />
                    </View>
                    <Text
                      style={{
                        color: "#fff",
                        fontWeight: "500",
                        fontSize: 12,
                      }}
                    >
                      Highlight
                    </Text>
                  </TouchableOpacity> */
}
